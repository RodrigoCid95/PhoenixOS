/* let dir = await window.showDirectoryPicker({mode: 'readwrite'})
let entries = await dir.entries()
for await (const [name, item] of entries) {
  console.log(name, item instanceof FileSystemDirectoryHandle ? 'Directorio' : 'Fichero')
} */