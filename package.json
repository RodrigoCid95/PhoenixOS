{
  "name": "phoenixos",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "vercel dev",
    "build:app": "pb build --input app --output ./public/js/apps/com.app.test --manifest",
    "build:core": "pb build --input core --output ./public/js/core",
    "build:os": "pb build --input os --output ./public/js/os",
    "build:all": "npm run build:app && npm run build:core && npm run build:os"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.18.2",
    "phoenix-builder": "github:RodrigoCid95/Phoenix-Builder",
    "socket.io-client": "^4.7.0"
  }
}
